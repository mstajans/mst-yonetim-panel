import React from 'react'
import ReactDOM from 'react-dom/client'
import AdminPanel from './App.jsx'
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode><AdminPanel /></React.StrictMode>
)
